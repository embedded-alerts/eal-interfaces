[package]
name = "eal-semantic-contracts"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"
description = "Versioned multi-view embedding input contracts for Embedded Alerts"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
