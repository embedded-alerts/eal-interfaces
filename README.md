# eal-interfaces

Canonical Embedded Alerts OpenAPI, AsyncAPI, JSON Schema, embedding, rule, and event contracts.

Initialized through `DEN-1949` as a testable `interfaces` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
